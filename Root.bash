return users bash32 data:app set true
echo "failed"
echo fsck.minix
e2freefrag start-stop-daemon
factor accessdb
c++filt g++
c++filt factor
caller faillog
factor false
h2xs fc-scan
then factor is false set if local +-4 file while task set then taskset for dev 
bash -c "echo $1"
